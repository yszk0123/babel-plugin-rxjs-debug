combineLatest([observable]).subscribe(() => {});

function foo() {
  combineLatest([observable]).subscribe(() => {});
}

class Foo {
  bar() {
    combineLatest([observable]).subscribe(() => {});
  }
}
